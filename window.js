const scriptName = "window K2048";

const { K2048 } = require("K2048_window");

/**
 * @param {string} room
 * @param {string} msg
 * @param {string} sender
 * @param {boolean} isGC
 * @param {object} replier
 * @param {function} replier.reply
 * @param {object} iDB
 * @param {function} iDB.getProfileHash
 * @param {string} pN
 */
function response(room, msg, sender, isGC, replier, iDB, pN) {
  
}